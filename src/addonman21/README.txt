Files are backported from anki-2.1.5 src

This addon must be loaded first, so the first file is named 1111111111.py in the main directory.

Each addon must have it's own folder with a config.json file.

